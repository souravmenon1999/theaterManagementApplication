'use client';

import Template from "../Template";
 

export default function userRegistration({
    children,
  }: {
    children: React.ReactNode
  }) {
    return (
      
        <Template>
        {children}
        </Template>
      
    
    )
  }